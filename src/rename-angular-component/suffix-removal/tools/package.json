{
  "name": "angular-rename-tools",
  "version": "1.0.0",
  "description": "Tools for renaming Angular files and updating references",
  "main": "dist/rename-angular-files.js",
  "bin": {
    "rename-angular-files": "dist/rename-angular-files.js"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rm -rf dist",
    "prebuild": "npm run clean"
  },
  "devDependencies": {
    "@types/node": "^18.0.0",
    "typescript": "^4.6.2"
  },
  "dependencies": {}
}
